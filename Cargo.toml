[package]
name = "bme680-rs"
version = "0.1.0"
edition = "2024"

[features]
default = []
embedded-hal = ["dep:embedded-hal"]

[dependencies]
paste = "1.0.15"
embedded-hal = { version = "1.0.0", default-features = false, optional = true }